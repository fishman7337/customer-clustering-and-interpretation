# Data Folder

Place local datasets here. Raw data is ignored by git to avoid committing customer-level
records accidentally.

Expected default input:

```text
data/raw/CA2-Customer-Data.csv
```

Use `data/processed/` for derived datasets if they are needed locally. Document important
data changes in `docs/DATA.md`.
