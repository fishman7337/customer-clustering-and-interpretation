# Customer Segmentation Package

This package contains the reusable Python implementation of the clustering workflow.

## Modules

- `config.py`: project paths, schema names, and constants.
- `data.py`: data loading, schema validation, and preprocessing.
- `modeling.py`: K-Means pipeline training, evaluation, and model persistence.
- `cli.py`: command line interface for training and exporting artifacts.
