# Source Code

This folder contains the installable Python package for the customer segmentation workflow.

The package is configured through `pyproject.toml` and installed in editable mode during
development with:

```powershell
python -m pip install -e ".[dev,notebook]"
```
