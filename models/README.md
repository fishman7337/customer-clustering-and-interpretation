# Models Folder

Generated model files are written here and ignored by git.

Default CLI output:

```text
models/customer_segmentation_kmeans.joblib
```

Regenerate the model with:

```powershell
customer-segmentation train --data data/raw/CA2-Customer-Data.csv
```
